# 04-filesystem-routing

```bash
bun index.ts
```

## Entry

```
http://localhost:4000/index.ts
```

- Similar to static files, but instead we execute the file
  - Open index.ts
- Using only node apis. This could run in node.
- No dependencies (except for typescript)
